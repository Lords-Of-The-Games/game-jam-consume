name: Godot Build Check

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: barichello/godot-ci:4.2.1

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Print Godot version
        run: godot --version

      - name: Run headless build
        run: godot --headless --build-solutions
